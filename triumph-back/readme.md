To run sqlite console use `./sqlite3 data.db`. Use SQL syntax in `sqlite>`. To see all the tables use `.tables` in `sqlite>`. To see help type `.help`.

Use `npm run build` in `./client` to build front end
